import React from 'react';
import ReactDOM from 'react-dom';
import App from "./components/App"

ReactDOM.render(<App />, document.getElementById("root"))

// Σε αυτό το αρχείο κάνουμε import το App για να γίνει render στην αρχική
// σελίδα της εφαρμογής στο index.html και πιο συγκεκριμένα στο div με id root
